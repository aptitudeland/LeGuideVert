<div>
  <%= link_to "Retour", garden_path(@garden) %>
</div>
<div>
<h1><%= @step.order %> - <%= @step.name %></h1>
</div>

<div>
  <% @tasks.each do |task| %>
    <%= task.order %> - <%= task.name %> <br>
    <%= task.description %>
    <br>
  <% end %>
  <br>
</div>


<div>
  <%= simple_form_for(@garden_step) do |f| %>
    <div class="d-none">
      <%= f.input :status, input_html: {value: "En attente"}%>
    </div>
    <%= button_tag(type: 'submit', class: " btn btn-light border") do %>
      <i class="icon-ok icon-white"></i> En attente
    <% end %>
  <% end %>

  <%= simple_form_for(@garden_step) do |f| %>
    <div class="d-none">
    <%= f.input :status, input_html: {value: "En cours"}%>
  </div>
      <%= button_tag(type: 'submit', class: "btn btn-warning border") do %>
        <i class="icon-ok icon-white"></i> En cours
      <% end %>
  <% end %>

  <%= simple_form_for(@garden_step) do |f| %>
    <div class="d-none">
      <%= f.input :status, input_html: {value: "Terminé"}%>
    </div>
      <%= button_tag(type: 'submit', class: "btn btn-success border") do %>
        <i class="icon-ok icon-white"></i> Terminé
      <% end %>
    <% end %>
</div>
